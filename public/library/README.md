# Free Resource Library — downloadable files

Drop published resource PDFs here, then set the matching `file` in `src/lib/library.ts`.

Example: add `first-24-hours.pdf` here, then in src/lib/library.ts set
`file: 'first-24-hours.pdf'` on the matching entry. The card flips from
"Coming soon" to a gated "Download" automatically.
